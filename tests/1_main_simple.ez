program main_simple
//+ test commentaires

include "sstream"

procedure hello_world()
print "Hello World !"
end

procedure checkTrueness(b is boolean)
    if b
    then print "value is true"
    else print "value is false"
    end if
end

main_simple
/*
this is

a multiple lines comment

*/
    print "First statement"

    hello_world

    //this is a test
    print "Second one"

    a is integer = 4
    b is boolean
    c is array[3] of integer
    name is string

    for i is integer in 0..2 step 1 do
        print "for looping, i : " i
        print "value of c[" i "]: " c[i]
    end

    while a - 1 * 2 > 0 do
        a = a -1
        b = true
        print "a : " a
    end while

    checkTrueness(b)

    if b or true
    then print "'or' operator working"
    else print "'or' operator not working"
    end if

    vect is vector of integer
    vect.push_back(3)

    
    print "value of vect[" 0 "]: " vect[0]
    print "value of vect.at(" 0 "): " vect.at(0)

    vect2 is vector of integer = { 1, 2 }

    l is list of string

end
